export function ResetTrackSelection() {
    return (
        <>
            <button onClick={() => {
                // setSelectedTrackId(null);
            }}>Reset selection
            </button>
        </>
    )
}